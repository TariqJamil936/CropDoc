# CropDoc — Streamlit application dependencies.
# Install with: pip install -r requirements.txt

streamlit>=1.37          # st.navigation / st.Page multipage routing
openai>=1.30
anthropic>=0.40           # optional: final diagnosis synthesizer, see agents/claude_agent.py
python-dotenv>=1.0
bcrypt>=4.1

# RAG
faiss-cpu>=1.8            # vector store (see ARCHITECTURE.md for why not ChromaDB)
pypdf>=4.0
python-docx>=1.1
pandas>=2.0

requests>=2.31             # API Agent (Open-Meteo weather)

# Vision model / Grad-CAM — pinned together deliberately. torch 2.2.2 only supports the
# NumPy 1.x ABI; numpy>=2 or a newer opencv-python-headless build (which requires numpy>=2)
# will break Grad-CAM with "Numpy is not available". Keep these in lockstep.
torch==2.2.2
torchvision==0.17.2
numpy<2
opencv-python-headless<4.10
Pillow>=10.0
